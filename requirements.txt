# Pinned for reproducibility (same versions used to build + test this prototype).
# Python 3.11+ recommended (the Docker image uses python:3.11-slim).

# --- Phase 1: ingestion, validation, metric engine ---
duckdb==1.5.3
pandas==3.0.3
pydantic==2.13.4
pytest==9.1.0

# --- Phase 2/3: deck generator, LLM narrative, conversational agent ---
matplotlib==3.11.0
python-pptx==1.0.2
streamlit==1.58.0
langchain==1.3.9
langchain-anthropic==1.4.6
python-dotenv==1.2.2
