# --- Phase 1: ingestion, validation, metric engine ---
duckdb>=1.1.0
pandas>=2.2.0
pydantic>=2.7.0
pytest>=8.0.0

# --- Phase 2/3: deck generator, LLM narrative, conversational agent ---
# (installed now so the full prototype runs end-to-end)
matplotlib>=3.8.0
python-pptx>=1.0.0
streamlit>=1.40.0
langchain>=0.3.0
langchain-anthropic>=0.3.0
python-dotenv>=1.0.0
